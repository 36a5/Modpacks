# al Shabab — Canary configuration
#
# mixin.ai.poi: Canary merges PoiManager#getInRange, which makes Valkyrien Skies'
# feature.poi.MixinPOIManager fail to inject and hard-crashes the server at world
# load (MixinApplyError). Upstream: ValkyrienSkies/Valkyrien-Skies-2#1901.
# Disabling this single Canary mixin package keeps every other Canary optimization.
mixin.ai.poi=false
