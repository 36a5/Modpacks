# Ghost mode ON: fake "left the game" message + hide from tab list (Vanishmod),
# then spectator for noclip flying. Voice chat keeps working (SVC spectator_interaction=true).
vanish
gamemode spectator @s
